{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar OpenMP",
            "type": "shell",
            "command": "g++",
            "args": [
                "-fopenmp",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compilación con soporte para OpenMP"
        }
    ]
}
